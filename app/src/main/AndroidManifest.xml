<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.waisblut.workouttimer"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="20"/>

    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_gym"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.waisblut.workouttimer.view.ActivityMain"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name="com.waisblut.workouttimer.service.MyIntentService"
            android:exported="false">
        </service>
        <receiver android:name="com.waisblut.workouttimer.view.FragmentMain$ResponseReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MESSAGE_PROCESSED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>